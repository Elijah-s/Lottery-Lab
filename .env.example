# Lottery Lab — example environment file.
#
# The real application stores the LLM API key in the macOS Keychain
# (see PR7). No secrets are loaded from this file.
#
# Keep this file as a placeholder and never commit a real `.env`.
